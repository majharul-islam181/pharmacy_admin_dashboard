import 'package:flutter/material.dart';
import '../../view/crm_view.dart';

class CrmPage extends StatelessWidget {
  const CrmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const CrmView();
  }
}
